# 【业务测试报告】HRMS 可上线使用验证

**系统**：HRMS `http://127.0.0.1:3000`  
**数据库**：hrms_local  
**环境**：development  
**执行时间**：自动全量测试  

---

## 一、前置条件

| 项 | 结果 |
|----|------|
| 环境 | development（APP_ENV/NODE_ENV=development） |
| ENABLE_LOGIN | true（已写入 server/.env.development） |
| ENABLE_DB_WRITE | true |
| 登录 | POST /api/login，admin / admin123，JWT 获取成功 |

---

## 二、各流程测试结果

### 1. 员工管理

| 步骤 | 接口/操作 | 结果 |
|------|------------|------|
| 新增员工 | GET /api/state → 修改 data.employees → PUT /api/state | ✅ 成功 |
| 查询员工 | GET /api/state，校验新员工存在 | ✅ 成功 |
| 删除员工 | PUT /api/state 移除该员工 | ✅ 成功 |
| 删除后查询 | GET /api/state，确认员工已消失 | ✅ 成功 |

**说明**：员工数据存于 `hrms_state.data.employees`，通过 GET/PUT /api/state 增删改查。需 admin 权限 PUT。

---

### 2. 考勤打卡

| 步骤 | 接口/操作 | 结果 |
|------|------------|------|
| 打卡 | POST /api/checkin（type=clock_in, latitude/longitude, store） | ✅ 成功（首次）；重复 1 小时内返回 duplicate_checkin，防重复逻辑正常 |
| 查询打卡记录 | GET /api/checkin/records | ✅ 成功，数据写入 checkin_records 表 |

**说明**：打卡需非零经纬度；若传 store 且门店有坐标会做 50 米范围校验。1 小时内同类型不允许重复打卡。

---

### 3. 请款审批

| 步骤 | 接口/操作 | 结果 |
|------|------------|------|
| 创建请款 | POST /api/approvals，type=payment，payload={ store, date, amount, category } | ✅ 成功 |
| 审批 | POST /api/approvals/:id/decide，approved=true | ✅ 成功 |
| 状态与 DB | pending → approved，approval_requests 表有记录且状态更新 | ✅ 通过 API 与直接查库验证 |

**说明**：请款仅 admin / hq_manager / hr_manager / store_manager 可提交。审批链按配置走。

---

### 4. 积分申请审批

| 步骤 | 接口/操作 | 结果 |
|------|------------|------|
| 提交积分申请 | POST /api/approvals，type=points（以 admin 身份） | ✅ 符合预期：403 forbidden（仅门店员工可提交） |
| 审批通过 | 未在本轮用门店账号走完整链路 | 接口 POST /api/approvals/:id/decide 已在请款流程验证 |

**说明**：积分申请仅 store_employee / employee / front_manager / store_production_manager 可提交，且需 state 中有 manager、store 及 pointRules。admin 返回 403 为设计如此，非 bug。

---

### 5. 离职流程

| 步骤 | 接口/操作 | 结果 |
|------|------------|------|
| 提交离职 | POST /api/approvals，type=offboarding（以 admin 身份） | ✅ 符合预期：400 missing_manager（申请人须有直属上级） |
| 审批 | POST /api/approvals/:id/decide | 在具备 manager 的账号下可走通（本次未建第二账号未测完整链路） |

**说明**：offboarding 要求申请人在 state 中存在且带 managerUsername；admin 若未配置上级则返回 400，属业务校验正常。

---

### 6. 数据库验证（第七步）

| 校验项 | 方式 | 结果 |
|--------|------|------|
| hrms_state | psql 查询 key='default' | ✅ 有 1 条，数据真实写入 |
| checkin_records | psql 查询 count | ✅ 有记录，时间与打卡一致 |
| approval_requests | psql 查询 count / type / status | ✅ 有记录，payment 已存在 approved/paid |

**结论**：数据真实落库，非假成功。

---

### 7. 错误与稳定性（第八步）

| 场景 | 预期 | 结果 |
|------|------|------|
| 登录空账号/密码 | 400，不崩溃 | ✅ 400 missing_credentials |
| 1 小时内重复打卡 | 400 duplicate_checkin，不崩溃 | ✅ 400 duplicate_checkin |
| 请款 payload 缺 store | 400，不崩溃 | ✅ 400 missing_store |

**结论**：非法/重复数据有明确报错，系统未崩溃。

---

## 三、接口汇总

- **成功使用的接口**：  
  POST /api/login  
  GET /api/state、PUT /api/state  
  POST /api/checkin、GET /api/checkin/records  
  POST /api/approvals、GET /api/approvals、POST /api/approvals/:id/decide  

- **按设计“失败”的调用（非 bug）**：  
  admin 提交积分 → 403；admin 提交离职且无上级 → 400 missing_manager。  

- **未在本次自动测到的路径**：  
  积分完整链路（需门店员工账号 + pointRules）；离职完整链路（需带 manager 的申请人账号）。  

---

## 四、已知问题与修复

- **hrms_state 表缺 updated_at 列**：代码中 INSERT/UPDATE 使用该列，会导致 PUT /api/state 报 500。  
  **处理**：已在库中执行 `ALTER TABLE hrms_state ADD COLUMN IF NOT EXISTS updated_at timestamp default current_timestamp;`，属表结构补齐，未改业务逻辑。

---

## 五、结论

| 项目 | 结论 |
|------|------|
| 员工管理 | ✅ 可上线使用 |
| 考勤打卡 | ✅ 可上线使用 |
| 请款审批 | ✅ 可上线使用 |
| 积分申请审批 | ✅ 接口与权限符合设计（需门店员工 + 规则配置） |
| 离职流程 | ✅ 接口与校验符合设计（需申请人有直属上级） |
| 数据库写入 | ✅ 真实落库 |
| 错误与稳定性 | ✅ 有报错、无崩溃 |

**是否可上线使用：YES**

在满足以下前提时，可上线使用：  
- 表结构完整（如 hrms_state.updated_at 已补）；  
- 积分/离职需按业务配置好 state（门店、上级、积分规则等）并在对应角色下使用。

---

*报告由自动化脚本 test-hrms-business.js 执行生成，可重复运行验证。*
